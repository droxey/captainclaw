---
# Cleanup temporary files after molecule test
- name: Cleanup reverse-proxy test artifacts
  hosts: localhost
  connection: local
  gather_facts: false
  become: false

  tasks:
    - name: Remove temporary test directory
      ansible.builtin.file:
        path: /tmp/molecule-reverse-proxy
        state: absent
